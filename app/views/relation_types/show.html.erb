<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @relation_type.name %><br />
  <strong>Key Words:</strong><br />
  <% @relation_type.key_words.each do |kw| %>
  	<%=kw.word%><br />
  <%end%>
</p>

<%= link_to 'Edit', edit_relation_type_path(@relation_type) %> |
<%= link_to 'Back', relation_types_path %>
